Put Fossil repos in here (each with a `.fossil` extension). 

Assuming the domain of the build cluster is `dev.local`, each file will map to a
URL as follows:

`repos/one.fossil` -> `http://fossil.dev.local:8080/one`
`repos/two.fossil` -> `http://fossil.dev.local:8080/two`

...and so on.
